{
  "name": "zenbit",
  "packageManager": "yarn@3.2.1",
  "devDependencies": {
    "@yarnpkg/sdks": "^3.0.0-rc.12",
    "typescript": "^4.7.4"
  },
  "scripts": {
    "build": "yarn workspace ui build && yarn workspace server build",
    "start": "yarn workspace server start:prod"
  },
  "workspaces": [
    "apps/*"
  ]
}
